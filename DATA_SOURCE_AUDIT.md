# Dashboard Data Source Audit

Last audited: 2026-04-29

This note maps each Observable dashboard CSV to the pages that use it, the apparent source pipeline in `C:\Users\doboy\OneDrive\Documents\KalshiData`, and whether it is currently raw Kalshi API-derived / near-live compatible.

## Summary

- The core Kalshi daily time-series used by the overview, volume, fees, and some category charts are now raw API-derived and near-live compatible.
- The new taker-side P&L files are raw API-derived and use `taker_side`.
- Migration scripts now exist for the remaining all-time / settlement-heavy Kalshi files; run the full orchestrator before treating the checked-in CSV snapshots as fully migrated.
- Competitor files are intentionally not Kalshi API-derived; they come from each competitor's raw export/scrape folders.
- The dashboard is still static once built. Local freshness comes from `npm run data:sync`, which copies fresh CSVs from `KalshiData\output` into `src\data`.

## CSV Inventory

| Dashboard CSV | Pages | Source / owner script | Current status | Notes |
| --- | --- | --- | --- | --- |
| `daily_overall.csv` | `index.md`, `volume.md`, `fees.md`, `competitors.md` | `R/rebuild_daily_timeseries.R`; incrementally updated by `R/near_live_update.R` | API-backed / near-live | Rebuilt from `data/raw_api/trade_data_YYYY-MM-DD.json`; synced into dashboard on 2026-04-28. |
| `daily_sports_vs_nonsports.csv` | `volume.md`, `fees.md`, `categories.md` | `R/rebuild_daily_timeseries.R`; incrementally updated by `R/near_live_update.R` | API-backed / near-live | Uses raw API trades plus series/category classification. |
| `daily_top_categories.csv` | `volume.md`, `categories.md` | `R/rebuild_daily_timeseries.R`; incrementally updated by `R/near_live_update.R` | API-backed / near-live | Wide tracked-series file from raw API. |
| `sports_market_type_daily.csv` | `categories.md` | `R/rebuild_daily_timeseries.R`; incrementally updated by `R/near_live_update.R` | API-backed / near-live | Uses raw API trades plus inline sports market-type classifier. |
| `taker_pnl_daily.csv` | `taker-pnl.md` | `R/build_taker_daily.R` | API-backed | Uses raw API `taker_side`, settlement cache, fees, category lookup. Not currently listed in `near_live_update.R`; likely needs scheduled rebuild/update if we want it continuously fresh. |
| `taker_notional_daily.csv` | `taker-pnl.md` | `R/build_taker_notional.R` | API-backed | Uses raw API `taker_side`; calculates taker dollars paid. Also needs scheduled update for full live behavior. |
| `taker_category_daily.csv` | `taker-pnl.md` | `R/build_taker_daily.R` | API-backed | Category-level P&L, but category-level taker-side volume is not currently exported. |
| `taker_category_summary.csv` | `taker-pnl.md` | `R/build_taker_daily.R` / `R/fix_catsum.R` | API-backed | Summary from `taker_category_daily.csv`. |
| `taker_sports_daily.csv` | `taker-pnl.md` | `R/build_taker_daily.R` | API-backed | Sports / non-sports taker P&L split. |
| `category_leaderboard.csv` | `categories.md` | `R/rebuild_leaderboards_api.R` | API migration implemented | Full-output CSV still needs to be regenerated by the orchestrator. |
| `market_leaderboard.csv` | `categories.md` | `R/rebuild_leaderboards_api.R` | API migration implemented | Uses raw API contracts/fees plus settlement cache for winners on full runs. |
| `calibration_three_way.csv` | `calibration.md` | `R/rebuild_settlement_outputs_api.R` | API migration implemented | Uses raw API trades plus binary settlement cache. |
| `parlay_pnl_net.csv` | `parlay.md` | `R/rebuild_settlement_outputs_api.R` | API migration implemented | Uses raw API trades plus binary settlement cache. |
| `trade_size_daily.csv` | `trade-size.md` | `R/rebuild_trade_size_api.R` | API migration implemented for Kalshi | Writes combined Kalshi / Polymarket / ForecastEx file; competitor rows still use raw competitor files. |
| `competitor_daily.csv` | `index.md`, `competitors.md` | `R/rebuild_competitor_daily_api_kalshi.R` | API migration implemented for Kalshi | Replaces Kalshi rows from API-backed `daily_overall.csv`; competitor rows remain from existing raw competitor exports. |
| `forecastex_categories_daily.csv` | `forecastex.md` | `R/forecastex_categories_daily.R` | Competitor source | Derived from `data/raw_forecastex`; not Kalshi API. |
| `forecastex_sports_split_daily.csv` | `forecastex.md` | `R/forecastex_categories_daily.R` | Competitor source | Derived from `data/raw_forecastex`; not Kalshi API. |
| `polymarket_categories_daily.csv` | `polymarket.md` | `R/polymarket_categories_daily.R` | Competitor source | Derived from `data/raw_polymarket`; not Kalshi API. |
| `polymarket_sports_split_daily.csv` | `polymarket.md` | `R/polymarket_categories_daily.R` | Competitor source | Derived from `data/raw_polymarket`; not Kalshi API. |
| `nadex_categories_daily.csv` | `nadex.md` | `R/nadex_categories_daily.R` | Competitor source | Derived from `output/nadex_events_daily.csv`; not Kalshi API. |
| `nadex_sports_split_daily.csv` | `nadex.md` | `R/nadex_categories_daily.R` | Competitor source | Derived from `output/nadex_events_daily.csv`; not Kalshi API. |

## Migration Priorities

1. Run `R/rebuild_dashboard_api_outputs.R --full --skip-taker` to regenerate the migrated non-taker CSVs.
2. Run `R/build_taker_daily.R` and `R/build_taker_notional.R` when a full taker refresh is desired; these are included in the orchestrator unless `--skip-taker` is passed.
3. Run `npm run data:sync` from the dashboard repo to copy regenerated `KalshiData\output` files locally.
4. Run `npm run build` in the dashboard repo and spot-check the affected pages.

## Local Freshness Workflow

From the dashboard repo:

```powershell
npm run data:sync
npm run dev
```

For a build using the freshest available `KalshiData\output` CSVs:

```powershell
npm run build:fresh
```

Use `npm run data:sync:force` only when `KalshiData\output` should overwrite dashboard copies even if those output files are older.

## New API Rebuild Scripts

Run from `C:\Users\doboy\OneDrive\Documents\KalshiData`:

```powershell
& "C:\Program Files\R\R-4.5.2\bin\Rscript.exe" R/rebuild_dashboard_api_outputs.R --start=2026-04-20 --end=2026-04-28 --skip-taker
```

For a full rebuild:

```powershell
& "C:\Program Files\R\R-4.5.2\bin\Rscript.exe" R/rebuild_dashboard_api_outputs.R --full --skip-taker
```

The first settlement-aware run creates `data/processed/ticker_settlement_binary.rds`, a small yes/no settlement cache used by `R/rebuild_settlement_outputs_api.R` and `R/rebuild_leaderboards_api.R`.
