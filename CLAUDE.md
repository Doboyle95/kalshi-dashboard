# CLAUDE.md

Project context for Claude Code (and other agents) working on this repo.

## What this is

A public Observable Framework site — **Predict Charts** — covering US prediction
markets: Kalshi plus nine competitor venues, with cross-venue comparisons,
per-venue deep dives, a market explorer, and an AI data explorer.

Two hosts serve the same `main`:

- **https://predict-charts.com** — the primary site. A VM-side timer rebuilds it
  from `origin/main` roughly every 5 minutes, independent of GitHub Actions.
- **https://doboyle95.github.io/kalshi-dashboard/** — the GitHub Pages fallback,
  deployed by `.github/workflows/deploy.yml` on every push to `main`.

Both are excluded from search engines (`noindex` + `src/robots.txt`); direct
links still work.

## Stack

- [Observable Framework](https://observablehq.com/framework/), pinned to exactly
  `1.13.4` in `package.json`. Do not widen it to a range — `deploy.yml` runs
  `npm ci`, and a silent 1.x bump breaks the layout overrides in `src/styles.css`.
- Pages live in `src/*.md` and are authored in Markdown with embedded
  ```js fenced code blocks.
- Data is generated by a separate **private pipeline** and published through a
  read-only VM gateway as immutable, hash-verified generations. It is not
  checked into this repository.
- `src/components/remote-data.js` resolves `/dashboard-data/current.json`, then
  reads every CSV from that exact generation. Do not add `src/data` fallbacks:
  transport failure must remain visible to monitoring instead of serving stale
  snapshots.
- Everything renders client-side after fetching the configured remote data.

## Scripts

```bash
npm install        # once
npm run dev        # live preview at http://localhost:3000
npm run build      # build static site into dist/
npm run test:data-transport
npm run clean      # wipe build cache
```

## Repo layout

```
.
├── .github/workflows/
│   ├── deploy.yml                 # push to main (or manual dispatch) -> Pages
│   └── daily_briefing.yml         # 18:30 UTC: regenerate src/daily-briefing.json
├── observablehq.config.js         # masthead, SITE_MAP, VENUES, head[] injection
├── scripts/generate-daily-briefing.mjs
├── src/
│   ├── index.md                   # Briefing (home) — volume chart + daily AI briefing
│   ├── compare.md                 # Compare hub; compare-*.md are the modules
│   ├── categories-venues.md, parlay-venues.md, trade-size.md
│   ├── market-explorer.md, venues.md, chat.md (Ask Predict Charts)
│   ├── volume.md, categories.md, taker.md, fees.md, taker-pnl.md,
│   │                              # ^ Kalshi's modules; see VENUES in the config
│   │   parlay-analytics.md, parlay.md
│   ├── polymarket.md, forecastex.md, dkex.md, underdog.md, nadex.md,
│   │   prophetx.md, novig.md, rothera.md, cme.md   # one venue landing each
│   ├── robinhood.md, methodology.md
│   ├── chat-endpoint.json         # public HTTPS API origin (VM rewrites + pushes)
│   ├── daily-briefing.json        # generated daily; rendered in the home hero
│   └── components/                # remote-data.js, chart-actions.js, safe-markdown.js, …
├── CLAUDE.md                      # this file
└── TICKER-RULES.md                # authoritative Kalshi ticker conversion rules
```

`observablehq.config.js` is the single source of navigation. `SITE_MAP` + `VENUES`
drive the masthead, the contextual module rows, the mobile menu and the footer
pager, and a build-time check **fails the build** if any `src/*.md` has no menu
entry or any entry points at no page. Adding or removing a page means editing
that map in the same commit.

## Working with categories.md

`src/categories.md` contains the Individual Market Leaderboard. It has to turn
raw Kalshi `market_key` / `winner_ticker` strings into human-readable Market /
Winner cells.

There is no longer a "Highest-vol. strike" cell, and none should be added back
until the producer changes: `market_leaderboard.csv`'s `top_outcome` is set to
`winner_ticker` rather than measured, so it duplicated the Winner column beside
it and named the genuinely busiest outcome on only 364 of 926 resolvable
markets. `fmtStrike` / `TOP_OUTCOME_NAMES` in `ticker-names.js` are retained for
that eventual fix but are currently unused.

The conversion logic lives in these functions in the big `js` block that
starts with `// ── Category colors ──`:

| Function / dict | Purpose |
| --- | --- |
| `CAT_COLORS` | Display-category colors. Sports is split into Football / Basketball / Other sport. |
| `NFL_TEAMS`, `NBA_TEAMS`, `MLB_TEAMS`, `NHL_TEAMS`, `CFB_TEAMS`, `CBB_TEAMS`, `SOCCER_TEAMS`, `CRICKET_TEAMS`, `IPL_TEAMS`, `TENNIS_PLAYERS`, `GOLF_PLAYERS` | Sport-specific code → name lookups. |
| `getTeamsForMarket(mk)` | Routes a `market_key` to the correct team/player dict. Uses prefix matching. **Always use this instead of `ALL_TEAMS`** when the market is identifiable, to avoid cross-sport collisions (e.g. MLB `SEA` → "Mariners", not NFL `SEA` → "Seahawks"). |
| `parseGame(code, teamMap)` | Greedy 2-team parse (longest-match first). |
| `parseTicker(mk)` | Ticker → readable market name for the cases where Kalshi's `market_name` is blank or just echoes the key. |
| `MKT_NAME_FORCE` | **Takes priority** over Kalshi's `market_name`. Use for markets where Kalshi's label is wrong or missing useful context (e.g. World Series games, Super Bowl LX, 2025 Masters). |
| `MKT_NAME_OVERRIDES` | Fallback override used only when both Kalshi `market_name` and `i.market_name` are blank or equal to the ticker key. |
| `WINNER_OVERRIDES` | Hard-coded winner display (keyed on `winner_ticker`). |
| `WINNER_BY_MARKET` | Winner for markets where `winner_ticker` is empty but the market did settle. Keyed on `market_key`. |
| `TOP_OUTCOME_NAMES` | Human names for specific `top_outcome` strings (full ticker, including market_key prefix). |
| `fmtWinner(d)` | Runs `WINNER_OVERRIDES` → strips long rule text → uses `winner` field → strips `::` prefixes → falls back to decoding the suffix of `winner_ticker` via `getTeamsForMarket`. Finally falls back to `WINNER_BY_MARKET`. |
| `fmtStrike(top, mk)` | Runs `TOP_OUTCOME_NAMES` → Fed H0/Hn/Cn → days (`42D` → `42 days`) → date (`26MAR01` → `by Mar 1`) → spread (`SEA4` → `Seahawks -4`) → percentage (**only** for `POPVOTEMOV*` markets) → sport-aware team/player. |
| `getSportDisplayCategory(d)` | Splits Kalshi's `Sports` category into `Football` / `Basketball` / `Other sport` for coloring and filtering. |

### Important correctness rules — do NOT regress these

1. **Sport isolation.** MLB/NBA/NHL/NFL team codes overlap (SEA, LA, TOR, etc.).
   Always look up team codes via `getTeamsForMarket(mk)` — not `ALL_TEAMS` —
   unless the market is genuinely ambiguous.
2. **Percentages.** Only `POPVOTEMOV*` markets use bare numbers that mean
   percent. Bitcoin price strikes and Electoral College margin strikes are
   bare numbers — they are NOT percentages. Do not slap `%` on them.
3. **Fed decisions.** Kalshi labels "Hike 0bps" for holds — we render `Hold`
   via a metadata-fix branch in `fmtWinner`.
4. **Market rule text.** When Kalshi's `winner` column is >50 characters it's
   almost always market-rule text like "If X wins the match…". Strip it (see
   the `rawW.length > 50 ? ""` line).
5. **`market_name` equals `market_key`.** Some Kalshi markets have
   `market_name` populated but it's just an echo of the ticker. Treat those as
   blank — that's what `bestName` does.
6. **Parlays.** Any market_key starting with `KXMVE` is a Kalshi parlay
   ("multi-game extended"). Display as `Parlay`, not the raw ticker.

### When adding a new market / fixing a new ticker

See `TICKER-RULES.md` for the full conversion reference. High-level flow:

1. If Kalshi's `market_name` is correct as-is → do nothing.
2. If Kalshi's `market_name` is wrong/misleading → add to `MKT_NAME_FORCE`.
3. If blank / echoes ticker → add to `MKT_NAME_OVERRIDES`, OR teach
   `parseTicker` a new regex branch if it's a whole family of tickers.
4. If a new sport appears, add a new team/player dict and add a routing
   branch to `getTeamsForMarket`.
5. If the winner cell renders a code like `SEA` instead of `Seahawks`, first
   check the sport routing. If it's a one-off (PGA player added mid-season,
   etc.), add to the right dict.
6. Add winner / strike overrides to `WINNER_OVERRIDES`, `WINNER_BY_MARKET`,
   or `TOP_OUTCOME_NAMES` as needed.

## Workflow

- Default branch: `main`. Push to `main` → Pages deploys, and the VM picks the
  commit up for predict-charts.com within ~5 minutes.
- Work on feature branches and open PRs. Typical branch naming:
  `cleanup/<topic>`, `fix/<topic>`, `feat/<topic>`.
- A push made by a workflow using the default `GITHUB_TOKEN` does **not** trigger
  another workflow. Anything that commits to `main` from Actions must dispatch
  `deploy.yml` itself, the way `daily_briefing.yml` does.
- Before pushing: `npm run build` locally to catch Observable errors.
- Data generations publish out-of-band when the private pipeline runs. Never
  commit generated CSVs or recreate `src/data/`; validate transport-facing
  changes with `npm run test:data-transport` and the browser canary.

## Non-goals / out of scope

- No private producer or mutable data service in this repository. The external
  gateway is read-only.
- No mutation of Kalshi data — we only read and display the CSVs.
- No hosting of the private R pipeline in this repo.
- No "Ticker" column in the leaderboard (explicitly declined). Instead, raw
  ticker stays in the underlying data and the visible columns are
  human-readable.

## Confirmed ticker meanings (previously uncertain)

- `KXMASTERS-25`: 2025 Masters Tournament, McIlroy won.
- `KXPERFORMSUPERBOWLB-26-CAR`: Cardi B.
- `PRESPARTYFULL-24-REC-RPV`: "Who wins Popular Vote + Electoral
  College?" combined market. `-REC` = Republican Electoral College
  branch, `-RPV` = Republican Popular Vote outcome. The strike names
  (`-RPV` / `-DPV`) are rendered as "Republican" / "Democratic" per
  Kalshi's popular-vote phrasing.
- `KXSUPERBOWLAD-SB2026`: winner is "Various" (multiple brands ran AI
  ads including Google). Both the market-level and winner-ticker-level
  overrides resolve to "Various".
